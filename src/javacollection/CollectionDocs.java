package javacollection;

public class CollectionDocs {

	/*
	 * -----------java collection framework-----
	 * 
	 * used to store and process dynamic multiple data objects in java
	 * 
	 * 1>list -> dynamic array
	 * a> ArrayList  -> slow processing ->Better to use for data storing
	 * b>LinkedList  -> fast processing ->better to use for data processing
	 * 
	 * 
	 * 2>set -> collection of unique data/object
	 * a>HashSet  ->unique data only
	 * b>TreeSet  ->unique + stored data
	 * c>LinkedHashSet  ->unique + insertion in data
	 * 
	 * 
	 * 3>map -> data manage in key value pair
	 * a>hashMap  ->(k,v) pair
	 * b>TreeMap  ->(k,v) +stored data
	 * c>LinkedHashMap  ->(k,v) +insertion in data
	 */
}
